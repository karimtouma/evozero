coverage:
  status:
    project:
      default:
        target: auto
        threshold: 5%      # allow small dips; fail only on real regressions
    patch:
      default:
        target: 80%
comment:
  layout: "reach, diff, files"
  require_changes: true
